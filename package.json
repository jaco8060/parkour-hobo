{
  "private": true,
  "name": "parkour-hobo",
  "version": "0.0.0",
  "license": "BSD-3-Clause",
  "type": "module",
  "dependencies": {
    "@devvit/public-api": "0.11.10",
    "three": "^0.174.0"
  },
  "devDependencies": {
    "@types/three": "^0.174.0",
    "esbuild": "^0.25.1",
    "typescript": "5.3.2"
  },
  "scripts": {
    "build": "esbuild src/app.ts --bundle --outfile=webroot/app.js --format=iife --global-name=THREE"
  }
}
